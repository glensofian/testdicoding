{
  "name": "story-app",
  "version": "1.0.0",
  "description": "Story App SPA + PWA + Push + IndexedDB",
  "scripts": {
    "start": "webpack serve --mode development",
    "build": "webpack --mode production",
    "deploy": "npm run build && git add dist -f && git commit -m \"build deploy\" || echo ok && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "css-loader": "^7.1.2",
    "html-loader": "^5.1.0",
    "html-webpack-plugin": "^5.6.4",
    "style-loader": "^4.0.0",
    "webpack": "^5.93.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.4"
  },
  "dependencies": {
    "idb": "^8.0.0"
  }
}
